import crypto from "crypto"; export const config={api:{bodyParser:true}}; function sign(pem,ts,method,path){const k=pem.replace(/\\n/g,"\n");const s=crypto.createSign("SHA256");s.update(ts+method.toUpperCase()+path);s.end();return s.sign({key:k,padding:crypto.constants.RSA_PKCS1_PSS_PADDING,saltLength:crypto.constants.RSA_PSS_SALTLEN_DIGEST},"base64");} export default async function handler(req,res){res.setHeader("Access-Control-Allow-Origin","*");res.setHeader("Access-Control-Allow-Methods","GET,POST,DELETE,OPTIONS");res.setHeader("Access-Control-Allow-Headers","Content-Type,KALSHI-ACCESS-KEY");if(req.method==="OPTIONS"){res.status(200).end();return;} const path=req.query.path||"";const env=req.query.env||"prod";const base=env==="prod"?"https://api.elections.kalshi.com/trade-api/v2":"https://demo-api.kalshi.co/trade-api/v2";const keyId=process.env.KALSHI_KEY_ID||"";const pem=process.env.KALSHI_PRIVATE_KEY||"";const ts=Date.now().toString();const apiPath="/trade-api/v2"+path;const hdrs={"Content-Type":"application/json"};if(keyId)hdrs["KALSHI-ACCESS-KEY"]=keyId;if(pem&&keyId){try{const sig=sign(pem,ts,req.method,apiPath);hdrs["KALSHI-ACCESS-TIMESTAMP"]=ts;hdrs["KALSHI-ACCESS-SIGNATURE"]=sig;}catch(e){console.error("Signing error:",e.message);}} try{const opts={method:req.method,headers:hdrs};if(req.method!=="GET"&&req.method!=="HEAD"&&req.body){opts.body=JSON.stringify(req.body);}const r=await fetch(base+path,opts);const text=await r.text();let out;try{out=JSON.parse(text);}catch(e){out={raw:text};}res.status(r.status).json(out);}catch(e){res.status(500).json({error:e.message});}}
